package com.messenger.core.config;

import org.springframework.context.annotation.Configuration;

// CORS конфигурация отключена - обрабатывается на уровне Gateway
@Configuration
public class CorsConfig {
    // Все CORS настройки убраны, так как они обрабатываются в Gateway
}
